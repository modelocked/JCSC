% ===============================
% Frontmatter_Backmatter/Setup.tex
% ===============================

% --- Core packages ---
\usepackage{setspace}
\usepackage{csquotes}
\usepackage[left=40mm,right=20mm,top=25mm,bottom=25mm]{geometry}
\usepackage{graphicx}
\usepackage{xparse}
\usepackage{xcolor}   % enables \textcolor used in referencing style

% --- Hyperlinks and URL handling ---
\usepackage{hyperref}
\usepackage{xurl}                % allows URL line breaks
\usepackage[normalem]{ulem}      % provides \uline for underlined URLs
\hypersetup{breaklinks=true}

% --- Glossaries and index (AFTER hyperref) ---
\usepackage[acronym,nonumberlist,toc]{glossaries}
\makeglossaries
\setglossarysection{chapter}
\usepackage{imakeidx}
\makeindex[columns=2,title=Index,intoc]

% --- Bibliography (load biblatex once, globally) ---
\usepackage[
backend=biber,
style=authoryear,
sorting=nyt,
maxnames=3,
minnames=1,
giveninits=true,
uniquename=false,
dashed=false
]{biblatex}

% --- Project styles ---
\input{Style_Formatting/Document_Formatting/Document_Formatting.tex}
% Referencing_Formatting.tex MUST NOT contain another \usepackage{biblatex}
\input{Style_Formatting/Referencing_Formatting/Referencing_Formatting.tex}

% --- Bibliography resource control ---
\providecommand{\thesisbibfile}{}
\newcommand{\setthesisbibfile}[1]{%
	\renewcommand{\thesisbibfile}{#1}%
	\addbibresource{#1}%
}

% --- Glossary & acronym definitions ---
\input{Frontmatter_Backmatter/Glossary}

% --- TeXcount rules ---
%TC:envir figure 0 0
%TC:envir table  0 0
%TC:macro \printbibliography [ignore]
%TC:macro \printglossary     [ignore]
%TC:macro \printglossaries   [ignore]
%TC:macro \printindex        [ignore]
%TC:macro \cite [ignore]
%TC:macro \parencite [ignore]
%TC:macro \textcite [ignore]
%TC:macro \autocite [ignore]
%TC:macro \footfullcite [ignore]
%TC:macro \footnote [ignore]
%TC:macro \chapter [ignore]
%TC:macro \chapter* [ignore]
%TC:macro \section [ignore]
%TC:macro \subsection [ignore]
%TC:macro \subsubsection [ignore]
